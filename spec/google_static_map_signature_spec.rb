require "spec_helper"

describe GoogleStaticMapSignature do
  it "has a version number" do
    expect(GoogleStaticMapSignature::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
